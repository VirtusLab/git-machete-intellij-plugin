package com.virtuslab.gitmachete.graph.model;

/** Graph element indents branch element. */
public class PhantomElement extends BaseGraphElement {
  public PhantomElement(int upElementIndex) {
    super(upElementIndex, null);
  }
}
