package com.virtuslab.gitmachete.frontend.defs;

public final class FileTypeIds {
  private FileTypeIds() {}

  // Extracted so that frontend:ui doesn't need to depend on frontend:file.
  public static final String NAME = "Machete File";
}
