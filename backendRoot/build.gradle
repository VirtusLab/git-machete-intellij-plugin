apply plugin: 'io.freefair.lombok'
generateLombokConfig.enabled=false

dependencies {
  guice()
  vavr()
  junit()
  powerMock()
  slf4j()

  implementation project(':gitCoreJGit')
  implementation project(':branchLayoutImpl')
  implementation project(':backendImpl')
}
